private void Form1_Load(object sender, EventArgs e)
        {
            TreeNode treeNode = new TreeNode("Yagiz Varlik");
            treeView1.Nodes.Add(treeNode);
    
            treeNode = new TreeNode("Ali  Boyaci");
            treeView1.Nodes.Add(treeNode);

            treeNode = new TreeNode("Dorukhan Varlik");
            treeView1.Nodes.Add(treeNode);

            treeNode = new TreeNode("Gizem Erinç");
            treeView1.Nodes.Add(treeNode);

            TreeNode node2 = new TreeNode("Anne");
            TreeNode node3 = new TreeNode("Baba");
            TreeNode[] array = new TreeNode[] { node2, node3 };
            
            treeNode = new TreeNode("Aile", array);
            treeView1.Nodes.Add(treeNode);
        }
